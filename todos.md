[ ]: default theming should be light theme.
[ ]: the example app's node editor's graph should include  
     sockets with editable default values and nodes with 
	 properties.
[ ]: default values for sockets should be editable in-line in the node 
     itself.  if necassary it can be a button that pops up something.
[ ]: an a ctrl-alt-p hotkey to the example app to pop up the command palette thingy.
   - not sure what I called it in the codebase
[ ]: use proper menu APIs
[ ]: nodes should be draggable if you click on their (empty) backgrounds as well 
     as titles.
[ ]: right clicking should pan, the add menu 
     should be exposed to client code which can fold it into 
	 a main menu, a menu in the editor, etc.  the example app 
	 should have an add menu in its header.
[ ]: drive the example app over cdp and take screenshots of the node editor,
     then come up with better theme colors for the node editor.


[ ]: the 'replace' popup menu should not popup in a mode where 
     pointer up closes the menu.
[ ]: default values for sockets should be editable right next to the node 
     sockets they belong to, as is the case with most node editors.